<h1>Listing work_memos</h1>

<table>
  <tr>
    <th>Text</th>
    <th>Work</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @work_memos.each do |work_memo| %>
  <tr>
    <td><%= work_memo.text %></td>
    <td><%= work_memo.work_id %></td>
    <td><%= link_to 'Show', work_memo %></td>
    <td><%= link_to 'Edit', edit_work_memo_path(work_memo) %></td>
    <td><%= link_to 'Destroy', work_memo, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Work memo', new_work_memo_path %>
