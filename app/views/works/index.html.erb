<h1>Listing works</h1>

<table>
  <tr>
    <th>Serial num</th>
    <th>Music</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @works.each do |work| %>
  <tr>
    <td><%= work.serial_num %></td>
    <td><%= work.music_id %>(
    	<% if work.music != nil %>
    		<%= work.music.file_name %>
		<% else %>
			No data
		<% end %>
    	
    	)</td>
    <td><%= link_to 'Show', work %></td>
    <td><%= link_to 'Edit', edit_work_path(work) %></td>
    <td><%= link_to 'Destroy', work, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Work', new_work_path %>
